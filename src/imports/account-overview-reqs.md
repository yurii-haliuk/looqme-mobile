Account overview — вимоги

Дані на екрані

Plan підписки:

назва плану

статус (active/trial/past_due)

renewal date / закінчення trial

seats used/total

Usage summary (org-level) з прогрес-барами:

mentions

AI coding

AI assistants

pages

keywords (як count)

reset date/period (якщо є)

Active integrations summary:

підключено / з помилками / потребує перепідключення

Billing status:

ok / past due / failed

Last activity:

last login (org-level можна показати як останній логін будь-кого або конкретно поточного користувача — вибір реалізації)

last settings change (з audit)

last keyword change (з audit, без редагування)

Дії

Для Admin:

перейти у Billing для апгрейду

перейти в Users для керування seats

Для User:

тільки перегляд + переходи

Порожні/помилкові стани

якщо дані usage недоступні — показати error state + retry

